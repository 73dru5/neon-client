import React from 'react'

function App() {
  return (
    <div>
      <h1>Hello from React + Rails!</h1>
      <p>API calls to /api/v2 will work via the proxy.</p>
    </div>
  )
}

export default App